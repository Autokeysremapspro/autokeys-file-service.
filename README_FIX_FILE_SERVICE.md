# FIX AK Cloud File Service

Este parche restaura el AppShell propio del portal y neutraliza una ruta interna de Core que se copió por error.

Copiar encima del repositorio `autokeys-file-service`, commit y push.
No requiere SQL.
